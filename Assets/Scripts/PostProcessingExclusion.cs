using UnityEngine;
using UnityEngine.Rendering;

public class PostProcessingExclusion : MonoBehaviour
{
    //public VolumeProfile volumeProfile;
    //public LayerMask layerMask;

    //public void SetPPProperty()
    //{
    //    var volume = GetComponent<Volume>();

    //    if (volume == null)
    //    {
    //        volume = gameObject.AddComponent<Volume>();
    //    }

    //    volume.isGlobal = true;
    //    volume.sharedProfile = volumeProfile;

    //    var layerMaskField = typeof(Volume).GetType().GetField("m_LayerMask", System.Reflection.BindingFlags.NonPublic | System.Reflection.BindingFlags.Instance);
    //    print(layerMaskField);
    //    if (layerMaskField != null)
    //    {
    //        print("Executed");
    //        layerMaskField.SetValue(volume, layerMask);
    //    }
    //}
}
